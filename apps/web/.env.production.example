# Variable d'environnement du frontend en PRODUCTION (à saisir dans Vercel).
# Doit pointer vers l'URL PUBLIQUE de l'API (Railway) suivie de /api/v1.
NEXT_PUBLIC_API_URL="https://votre-api.up.railway.app/api/v1"
